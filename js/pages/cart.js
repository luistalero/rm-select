import {getCart,updateQuantity,removeFromCart,cartTotal} from '../core/cart.js';
const root=document.getElementById('cart-content');const money=v=>new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(Number(v||0));const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));
function render(){const cart=getCart();if(!cart.length){root.innerHTML='<div class="store-empty"><h2>Tu carrito está vacío</h2><p>Explora la colección y agrega tus productos.</p><a class="button button--primary" href="shop.html">Explorar colección</a></div>';return;}const subtotal=cartTotal();root.innerHTML=`<div class="cart-layout"><div class="cart-items">${cart.map(i=>`<article class="cart-item"><div><strong>${esc(i.product_name)}</strong><span>${esc(i.variant_name||'Única')} · ${esc(i.sku||'')}</span></div><div class="cart-qty"><button type="button" data-minus="${i.variant_id}">−</button><span>${i.quantity}</span><button type="button" data-plus="${i.variant_id}">+</button></div><strong>${money(Number(i.price)*i.quantity)}</strong><button class="cart-remove" type="button" data-remove="${i.variant_id}">Eliminar</button></article>`).join('')}</div><aside class="cart-summary"><p class="eyebrow">RESUMEN</p><div><span>Subtotal</span><strong>${money(subtotal)}</strong></div><div><span>Envío</span><span>Se calcula en checkout</span></div><hr><div><span>Total productos</span><strong>${money(subtotal)}</strong></div><a class="button button--primary" href="checkout.html">Continuar al checkout</a></aside></div>`;}
root.addEventListener('click',e=>{const minus=e.target.closest('[data-minus]'),plus=e.target.closest('[data-plus]'),remove=e.target.closest('[data-remove]');if(minus){const i=getCart().find(x=>x.variant_id===minus.dataset.minus);updateQuantity(i.variant_id,i.quantity-1);render();}if(plus){const i=getCart().find(x=>x.variant_id===plus.dataset.plus);updateQuantity(i.variant_id,i.quantity+1);render();}if(remove){removeFromCart(remove.dataset.remove);render();}});render();
